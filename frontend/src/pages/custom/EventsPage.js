import { useState } from 'react';

export default function EventsPage() {
	
	const [pageReady, setPageReady] = useState(true);
	return (
		<main id="EventsPage" className="main-page">
		</main>
	);
}
